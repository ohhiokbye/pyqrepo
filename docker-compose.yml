services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: cpyq_user
      POSTGRES_PASSWORD: cpyq_password
      POSTGRES_DB: cpyq
    ports:
      - "5433:5432"
    volumes:
    
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
